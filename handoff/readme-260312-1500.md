# Handoff - mcp - 2026-03-12 15:00

## Completed
- Fixed Naver copy formatting: markdown was being stripped before clipboard conversion
  - Root cause: `_strip_markdown(v["content"])` in app.py removed all **bold**, ##headings
  - Fix: removed _strip_markdown from version content (only applies to title now)
- blogToNaverHTML upgraded to Naver SE3-compatible format:
  - Font: 나눔고딕 (Naver default), 15px, line-height 2.2
  - Bold: inline style span instead of <b> tag (SE3 compatible)
  - Headings: proper font-size (17px for ##, 20px for #), bold, margin
  - Empty line handling: single spacer p only (no double spacers)
  - Paragraph bottom margin 2px (matches SE3 natural spacing)
- formatBlogContent: renders markdown for proper UI display (not raw **text**)

## Remaining
- None

## Context
- mcp server runs on port 5050 via `python web/app.py` in C:/work/mcp
- The clipboard copy puts both text/html (blogToNaverHTML) and text/plain in clipboard
- When pasting into Naver Blog editor, text/html is used → SE3 parses inline styles
