# Handoff - mcp - 2026-03-10 20:35

## Completed
- Fixed Naver blog URL crawling failure
- Root cause: Naver desktop blog (blog.naver.com/USER/POST_NO) uses JS rendering — requests gets empty shell
- Fix: added `_normalize_naver_blog_url()` that converts to PostView URL (server-side rendered)
  - blog.naver.com/USER/POST_NO → blog.naver.com/PostView.naver?blogId=USER&logNo=POST_NO
  - m.blog.naver.com/USER/POST_NO → same PostView URL
  - Query strings like ?trackingCode=... are ignored (regex only matches path)
- Also improved User-Agent and added Referer header for better crawl success
- Added explicit short-text check: raises error if extracted text < 100 chars

## Remaining
- None

## Next Steps
- Test: paste https://blog.naver.com/USER/POST_NO URL in 빠른 비교 테스트 or 비교 modal → should succeed
- Calibration analysis should now run properly

## Context
- fetch_url_text() in web/app.py now calls _normalize_naver_blog_url() before fetching
- PostView URL extraction yields ~10,000+ chars from Naver blog posts
- Other blog platforms (Tistory, WordPress etc.) work as-is with standard HTML scraping
