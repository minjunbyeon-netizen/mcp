# Handoff - mcp - 2026-03-10 09:20

## Completed
- Created `web/run_crawler.py` — Naver blog crawler with 4 functions: `get_blog_id`, `get_post_list`, `get_post_content`, `save_results`
- Flask server running at http://localhost:5050 (started via `python web/app.py`)
- Crawler loads successfully; Flask debug mode auto-reloads on file change

## Remaining
- None

## Next Steps
- Test blog collection with a real Naver blog ID via the web UI
- If login_required blocks API, test after logging in via Google OAuth

## Context
- run_crawler.py must be placed in `web/` directory (same level as app.py)
- app.py overrides OUTPUT_DIR externally: `_run_crawler.OUTPUT_DIR = str(BLOG_COLLECTIONS_DIR)`
- Mobile URL (`m.blog.naver.com`) used for content extraction to avoid iframe issues
- Flask server port: 5050, debug=True (auto-reload enabled)
