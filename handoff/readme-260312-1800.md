# Handoff - mcp - 2026-03-12 18:00

## Completed
- Rewrote html_style extraction in app.py: font map (all Naver SE3 class names), _to_px() for fsNN format, correct color source (c11.text_colors hex, not c12.color_examples text)
- Fixed accent color selection: added _is_gray() helper to skip near-gray hex (R=G=B < 15 threshold), picks colorful accent (#0078cb blue) instead of gray (#c2c2c2)
- Rewrote blogToNaverHTML in app.js: applies all html_style fields - font-family, font-size, line-height, accent_color (headings), highlight_color (==text==), bold_allowed (**text**), centerHeadings (text-align:center), image insertion at section boundaries
- Changed prompt: 3 versions to 1 "DNA 일치형" version; removed ** ## forbidden rule; added conditional bold/heading instructions
- Added extract_images_from_file() in app.py: PDF (PyMuPDF), DOCX (zipfile word/media/), direct image files; saved to /uploads/ persistent dir
- Preserved markdown in content: removed _strip_markdown(v["content"]) call; only title is stripped now

## Remaining
- No known bugs. Ready for user testing.

## Next Steps
1. Test full pipeline: upload PDF, select DNA, generate, click Naver copy, paste in Naver Blog editor
2. Verify: font family, font size, accent color on headings, bold text, center alignment, images embedded

## Context
- MCP server: port 5050, C:/work/mcp/web/app.py + app.js
- DNA: c11_visual_symbols (text_colors=hex, highlight_colors, center_align_usage, line_break_style), c12_typography (font_families=Naver SE3 class names, base_font_size=fsNN, bold_frequency=int)
- c12.color_examples = text descriptions (NOT hex). c11.text_colors = actual hex values.
- Accent color skips grays (R approx G approx B < 15) and body defaults (#333 #141414 etc)
- blogToNaverHTML uses ClipboardItem with text/html + text/plain for Naver Smart Editor 3
