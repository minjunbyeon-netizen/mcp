# Handoff - mcp - 2026-03-12 16:00

## Completed
1. 1 version only: prompt changed from 3 versions to 1 "DNA 일치형" version
2. Bold/heading allowed: removed "** 사용 금지" rule → AI can use **bold** and ## heading
   - Only applies if DNA bold_frequency >= 3
3. DNA-based HTML style: API response now includes html_style{font_family, base_font_size, heading_font_size, accent_color, bold_allowed, line_height}
   - Derived from c12_typography in DNA
   - Font names mapped to CSS font-family strings
4. blogToNaverHTML uses window._lastHtmlStyle for font/size/bold
   - Title font-size = base + 7px
   - h1 = heading + 3px, h2 = heading size
   - Gap lines → 4px spacer (matches Naver single blank line)
   - Images: prefer h1/h2 post-gap positions, fallback to even distribution
5. formatBlogContent also uses bold rendering

## Remaining
- None

## Context
- Server: python web/app.py in C:/work/mcp, port 5050
- Images saved in C:/work/mcp/web/uploads/ (persistent)
- If DNA has no bold (bold_frequency < 3), ** markers are stripped in display
- html_style is per-generation, not persisted
