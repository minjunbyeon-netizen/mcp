# Handoff - mcp - 2026-03-10 20:45

## Completed
- Replaced auto-save calibration with manual checklist selection system
- calibrate-from-url API: analyze only, no longer auto-saves
- New POST /api/blog/calibration/save endpoint: saves with user-selected items + custom prompt
- Frontend: analysis result rendered as numbered checkbox list (1,2,3...)
- Categories: 더 활용 (blue), 줄일 것 (red), 톤 변화/구조/분량 (gray), 특징 표현 (dark gray)
- "전체 선택" / "전체 해제" toggle buttons
- "선택한 항목 보정 등록" button - builds calibration_prompt from checked items only
- Both modal (비교 button) and 빠른 비교 테스트 panel use the same checklist

## Remaining
- None

## Next Steps
- Test: compare → see checklist → uncheck unwanted items → click 보정 등록 → verify saved

## Context
- renderCalibrationChecklist(a, containerId, meta) - main render function
- toggleAllCalItems(checked, containerId) - global
- saveSelectedCalibration(containerId) - builds prompt from selected, calls /api/blog/calibration/save
- CSS classes: .cal-item-row, .cal-item-check, .cal-item-badge, .cal-item-num, .cal-item-text
- meta stored on container._calMeta, items on container._calItems, analysis on container._calAnalysis
