# Handoff - mcp - 2026-03-10 20:30

## Completed
- Fixed "최근 생성 글 불러오기" bug in 빠른 비교 테스트 panel
- Button now calls GET /api/mypage/blogs when no in-session versions exist
- Shows a picker modal listing up to 20 most recent saved blogs (title + date)
- Clicking a blog loads its content via GET /api/mypage/blogs/<blog_id> into qc-ai-title and qc-ai-content
- If current session has in-memory versions (window._blogVersions), those are used first (original behavior)

## Remaining
- None

## Next Steps
- Test: go to 빠른 비교 테스트 → click "최근 생성 글 불러오기" → picker modal should appear with saved blogs
- Select a blog → title and content should fill in automatically
- Then enter a URL and run 비교 분석

## Context
- Bug was: window._blogVersions is session-only, empty on page reload
- Fix: app.js qc-load-latest handler now async, falls through to API fetch if no session versions
- Picker modal: position:fixed, z-index 9999, backdrop z-index 9998
- API: GET /api/mypage/blogs → items[]; GET /api/mypage/blogs/<id> → versions[0].title + versions[0].content
