# Handoff - mcp - 2026-03-13 17:00

## Completed
- Fixed duplicate image insertion in blogToNaverHTML (app.js)
  - Root cause: imgIdx (auto) and imgBlockSeq (explicit [이미지]) both started at 0, consuming same images[]
  - Fix: split image pool — auto uses images[0..autoImgCount-1], explicit blocks use images[autoImgCount..end]
- Confirmed no other conflicts in the codebase (OUTPUT_DIR paths, TTL cleanup, import aliases all clean)
- Committed (9bb0aa3), pushed, deployed to VPS — service active

## Remaining
- Gemini API key: old key spending cap exceeded, new key (AIzaSyB66...) has no billing connected — user to fix
- Google OAuth credentials not set (SSO disabled, dev@localhost auto-session in use)

## Next Steps
1. Fix Gemini API billing (raise spending cap or connect billing on new key)
2. Test full pipeline once API is restored: collect → DNA analysis → blog generation
3. Verify [이미지] marker placement and no-duplicate behavior in live test

## Context
- VPS: 137.184.82.157, port 5051, subpath /mcp/, systemd mcp.service active
- Image pool split: seqExplicitImgCount reserves tail of images[] for explicit [이미지] blocks
- [이미지:N] specific-index blocks reference images[N-1] directly (bypasses pool split)
- DNA analysis: gemini-2.0-flash; Blog generation: gemini-2.5-pro
