# Handoff - mcp - 2026-03-10 19:30

## Completed
- Built blog calibration loop system: AI draft vs approved version → auto-style improvement
- Each blog version now has "보정 등록" button (yellow, in action toolbar)
- Click → modal → paste approved blog title + body → Gemini analyzes differences
- Analysis result: do_more / do_less / tone_shift / structure_diff / key_phrases / calibration_prompt
- Calibration records saved to: ~/mcp-data/outputs/calibrations/CAL_*.json
- On next blog generation: loads last 3 matching calibrations (by style_template_id) → injects into AI prompt
- New sidebar item: 마이페이지 > 보정 기록 → list + delete calibrations
- New API endpoints:
  - POST /api/blog/calibrate → analyze + save
  - GET /api/blog/calibrations?style_template_id=... → list
  - DELETE /api/blog/calibration/<cal_id> → delete

## Remaining
- None

## Next Steps
- Test flow: generate blog → click "보정 등록" → paste approved blog → click "보정 분석 시작" → verify result
- Generate another blog with same style → check calibration is applied (look for "[실제 통과된 글 기반 보정 지침]" in prompt)
- Manage calibrations in: 마이페이지 > 보정 기록

## Context
- CALIBRATIONS_DIR: ~/mcp-data/outputs/calibrations/
- Calibration matching: by style_template_id (not blog_dna_id, for broader coverage)
- Max 3 calibrations loaded per generation (most recent)
- Calibration prompt injected right after [글쓰기 스타일 템플릿] section in AI prompt
