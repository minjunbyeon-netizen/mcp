# Handoff - mcp - 2026-03-13 16:00

## Completed
- DNA analysis model: gemini-2.5-pro → gemini-2.0-flash (cost reduction ~30-50x)
- Injected pre-computed measured stats (char/para/sentence counts) into analysis prompt
- Expanded c14_length_stats: median, min/max paragraphs, short/long post patterns, volume summary
- Expanded c16_image_media: min/max images, count range, opening/body/closing distribution, image-text ratio, caption examples
- Fixed port 5051 zombie process causing service failure
- Reverted app.py prompt changes that caused unexpected 3-version generation

## Remaining
- Google OAuth credentials not yet set (SSO disabled, dev@localhost auto-session in use)
- Image count is estimated by AI (not measured) — crawler doesn't collect image metadata

## Next Steps
1. Google OAuth: add GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SSO_ENABLED=true to VPS .env
2. Optionally: update crawler to record actual image count per post for more accurate c16 data

## Context
- VPS: 137.184.82.157, port 5051, subpath /mcp/, systemd mcp.service active
- Blog generation: gemini-2.5-pro (kept for quality)
- DNA analysis: gemini-2.0-flash (switched for cost)
- Measured stats injected as "실측 데이터" block — AI must use real numbers, not estimates
- Port 5051 zombie process issue: recurring problem, fix with lsof -ti :5051 | xargs kill -9
