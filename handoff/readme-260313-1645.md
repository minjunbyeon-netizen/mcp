# Handoff - mcp - 2026-03-13 16:45

## Completed
- run_crawler.py: collect actual image count per post (filters postfiles/blogfiles img tags)
- run_crawler.py: auto-cleanup old collection folders — keeps latest 3 per blog_id
- app.py: TTL cleanup of BLOG_* output files at startup (30-day expiry)
- app.py: image_count per post injected into DNA analysis measured stats block
- Committed (78a0deb), pushed, and deployed to VPS — service active (running)

## Remaining
- Gemini API key issue: old key spending cap exceeded, new key (AIzaSyB66...) has no billing connected
  - User decided to fix tomorrow; DNA analysis will fail until API is restored
- Google OAuth credentials not set (SSO disabled, dev@localhost auto-session in use)
- Duplicate image insertion bug: original attempt at fix (hasExplicitImgBlock) was reverted by user — may need cleaner solution once API is working and generation can be tested

## Next Steps
1. Fix Gemini API billing: either raise spending cap on old project or connect billing on AIzaSyB66... key
2. Test full pipeline: collect blog → DNA analysis (gemini-2.0-flash) → generation (gemini-2.5-pro)
3. Verify image_count data flows correctly from crawler through DNA c16_image_media
4. Optionally re-add font preference prompt (궁서체 → 나눔바른고딕) — carefully, as prev attempt caused "3가지 형태의 글" anomaly

## Context
- VPS: 137.184.82.157, port 5051, subpath /mcp/, systemd mcp.service active
- Gemini keys: check Google Cloud Console → billing → spending cap
- Port zombie fix: permanent via ExecStartPre in mcp.service
- DNA analysis: gemini-2.0-flash; Blog generation: gemini-2.5-pro
- image_count in style_meta: counts <img> with src containing postfiles/blogfiles/mblogthumb/blogimg
