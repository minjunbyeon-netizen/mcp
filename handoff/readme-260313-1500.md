# Handoff - mcp - 2026-03-13 15:00

## Completed
- Added [이미지]/[이미지:N] block parser handler → renders actual <img> from images[] array
- Added orphan block marker cleanup in mdInline(): strips stray [중앙]/[/중앙]/[우측]/[박스] etc.
- Updated font_map in app.py: added 나눔바른고딕, 나눔스퀘어; remapped 궁서/궁서체 → 나눔바른고딕
- Changed default font fallback from 맑은고딕 to NanumBarunGothic
- Updated generation prompt: prefer 나눔바른고딕/나눔스퀘어, added [이미지] marker usage guide
- Deployed to VPS (137.184.82.157), HTTP 200 confirmed

## Remaining
- Google OAuth credentials not yet set (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET missing from VPS .env)
- SSO currently disabled → dev@localhost auto-session in use

## Next Steps
1. User provides Google OAuth client ID + secret
2. Add to VPS: ssh vps-dev → nano /home/dev/work/mcp/.env → add GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SSO_ENABLED=true
3. Register redirect URI http://137.184.82.157/mcp/callback in Google Cloud Console
4. sudo systemctl restart mcp → test real Google login

## Context
- VPS: 137.184.82.157, user dev, port 5051, subpath /mcp/
- systemd: /etc/systemd/system/mcp.service, enabled + active
- nginx: /etc/nginx/sites-enabled/work, /mcp/ → localhost:5051
- app venv: /home/dev/work/mcp/.venv
- [이미지] markers: block parser now handles, sequential counter imgBlockSeq tracks inline usage
- Orphan block markers (중앙/우측/들여쓰기/박스) stripped at start of mdInline() to prevent text leakage inside boxbg/box content
- Font: default now NanumBarunGothic, 궁서체 remapped, prompt explicitly tells AI to avoid decorative fonts
- total inline markers now: ~45 (39 original + 6 new)
