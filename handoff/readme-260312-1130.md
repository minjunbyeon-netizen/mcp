# Handoff - mcp - 2026-03-12 11:30

## Completed
- Full page restructure: 3-panel sidebar layout (Blog Collect / Blog Write / Style Update)
- Panel 1 (블로그 수집): URL input → collect → DNA analyze → show result → go to write
- Panel 2 (블로그 글 작성): DNA select + file upload (PDF/HWP/DOCX/XLSX/image) + URL/text input + options + generate → preview + save
- Panel 3 (블로그 스타일 업데이트): DNA card list → click → persona tag toggle (ON/OFF) → save active_tags
- Backend: added `/api/mypage/dna/<dna_id>/tags` PATCH endpoint
- Backend: `active_tags` now passed through to generate prompt
- Backend: added Excel (.xlsx/.xls) file support in utils.py

## Remaining
- None (structural redesign complete)

## Next Steps
- Test full flow: collect → DNA analyze → write with DNA → style tag toggle
- Optionally: add style template selector to Panel 2 (currently hardcoded to 'informational')
- Optionally: add Google Docs export button to version block in Panel 2

## Context
- Flask server runs on port 5050 (started via: cd web && python app.py)
- DNA files: C:/Users/USER/mcp-data/outputs/dna/DNA_*.json
- active_tags saved as array [{id, label, cat}] in DNA JSON
- Panel 3 tag extraction: extractPersonaTags() in app.js reads c1_tone through c10_visual_formatting
- Old style.css/app.js completely replaced — do not revert
