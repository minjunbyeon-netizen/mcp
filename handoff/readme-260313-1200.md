# Handoff - mcp - 2026-03-13 12:00

## Completed
- Committed and pushed 30+ inline markers + 진하게 support (commit 83b13d4)
- Implemented Google email whitelist access control (commit 10c9135)
  - `_load_allowed_emails()` reads ALLOWED_EMAILS env var + `allowed_emails.txt` file
  - `_save_allowed_emails()` persists to `C:/work/mcp/allowed_emails.txt`
  - Login callback: checks email against whitelist; clean 403 denial page if denied
  - Empty list = open access (backwards compatible with current state)
  - Admin API: GET/POST/DELETE `/api/admin/allowed-emails` (login_required)
  - Self-removal protection: cannot remove own email
  - Frontend: new "접근 관리" nav panel with email list + add/remove UI

## Remaining
- Currently no emails in allowed_emails.txt → anyone with Google account can login
- User should add their own email first via 접근 관리 panel before restricting access

## Next Steps
1. Login to app → go to "접근 관리" panel
2. Add your own email first (e.g. yourname@gmail.com)
3. Add other allowed collaborators' emails
4. Once list is non-empty, only those emails can login

## Context
- allowed_emails.txt is gitignored-safe (not committed, lives on server)
- ALLOWED_EMAILS env var also works for server/Docker deployments
- Domain whitelist (ALLOWED_DOMAINS env var) still works alongside email whitelist
- Access logic: allowed if (email in ALLOWED_EMAILS) OR (domain in ALLOWED_DOMAINS) OR (both lists empty)
