# Handoff - mcp - 2026-03-10 20:00

## Completed
- Changed calibration flow: URL input instead of manual text paste
- Recent blogs table now has "비교" button (yellow) between Docs and 삭제
- Click "비교" → modal with URL input field → server crawls URL → compares with saved AI blog
- Analysis result shows: 현재 일치율 (%), do_more, do_less, tone_shift, key_phrases, calibration_prompt
- Calibration prompt auto-applied on next blog generation with same style_template_id
- 보정 기록 panel updated: shows 일치율 % column with color coding (green/yellow/red)
- saveBlogVersion() now tracks _lastSavedBlogId so version tab "보정 등록" button works too
- save_blog API now returns blog_id in response

## How the full flow works
1. Generate blog → Save (저장 버튼)
2. Go to 최근 작성한 글 → click "비교" on that row
3. Paste the actual published blog URL → click "비교 분석 시작"
4. See similarity score + analysis
5. Next blog generation with same style → calibration auto-applied

## Next Steps
- Test: generate → save → 비교 → URL → check score
- Multiple calibrations accumulate → score should rise over time
- View calibration history: 마이페이지 > 보정 기록

## Context
- POST /api/blog/calibrate-from-url: {blog_id, approved_url, style_template_id, blog_dna_id}
- GET /api/blog/calibrations: list, supports ?style_template_id= filter
- CALIBRATIONS_DIR: ~/mcp-data/outputs/calibrations/
- Calibrations matched by style_template_id, max 3 most recent applied per generation
