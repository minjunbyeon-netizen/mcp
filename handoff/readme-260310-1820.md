# Handoff - mcp + email-persona - 2026-03-10 18:20

## Completed
- Built C:\work\email-persona\web\ Flask app (port 5051) — persona extraction + email drafting + team view
  - app.py: 3 features: /api/persona/extract, /api/email/generate, /api/personas
  - index.html, style.css, app.js: full UI (sidebar nav, 3 panels)
  - start.bat: double-click to launch
  - Loads GEMINI_API_KEY from C:\work\mcp\.env automatically
- Added URL crawling to mcp blog generation (port 5050):
  - "자료 URL" field: press release web page or PDF URL → fetched and added to material
  - "참고 블로그 URL" field: crawled blog post used as style reference in AI prompt
  - Modified: web/index.html (STEP 3 UI), web/app.js (form submission), web/app.py (fetch_url_text function + generate_blog params)

## Remaining
- None

## Next Steps
- Start both servers:
  - App 1 (blog): cd C:\work\mcp && python web/app.py → http://localhost:5050
  - App 2 (email-persona): cd C:\work\email-persona && python web/app.py → http://localhost:5051
- Test email-persona: upload kakao TXT → extract persona → write email
- Test mcp URL crawling: paste a press release URL or reference blog URL → generate blog

## Context
- email-persona app files: C:\work\email-persona\web\ (app.py, index.html, style.css, app.js)
- PERSONA_DIR: C:\work\email-persona\output\personas\
- GEMINI_API_KEY loaded from C:\work\mcp\.env (auto-fallback)
- mcp new field in STEP 3: press-url (id) + reference-blog-url (id)
- fetch_url_text() in mcp app.py: handles HTML pages + PDF URLs
- reference_blog_text injected into AI prompt as "[참고 블로그 스타일 샘플]" section
