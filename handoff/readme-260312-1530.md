# Handoff - mcp - 2026-03-12 15:30

## Completed
- Image extraction from uploaded material (PDF/DOCX/image files)
  - PDF: PyMuPDF extracts embedded images (skip <10KB thumbnails/icons)
  - DOCX: zipfile extracts word/media/ images
  - Image files (.jpg/.png etc.): saved directly
  - All saved to C:/work/mcp/web/uploads/ with UUID names
- Flask serves /uploads/<filename> statically
- API response includes "images": ["/uploads/xxx.jpg", ...] alongside versions
- Frontend stores window._lastImages as full localhost:5050 URLs
- Image thumbnail panel shown above version blocks after generation
- blogToNaverHTML(title, content, images):
  - Collects paragraph blocks first
  - Inserts images at heading/gap boundaries, evenly distributed
  - Leftover images appended at end
  - <img> tags center-aligned, max-width:100%

## Remaining
- None

## Context
- Naver Blog can load img src="http://localhost:5050/uploads/..." because img tags are CORS-free
- On Naver save, editor uploads images to Naver CDN automatically
- Uploads dir is persistent (not cleaned up) - may need periodic cleanup
